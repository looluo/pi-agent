{
  "name": "pi-agent-app",
  "version": "0.3.0",
  "private": true,
  "description": "Desktop app wrapper for Pi Agent Web",
  "type": "module",
  "scripts": {
    "prepare:icons": "node scripts/create-icons.mjs",
    "prepare:webapp": "node scripts/build-webapp.mjs",
    "tauri": "tauri",
    "dev": "npm run prepare:icons && npm run prepare:webapp && tauri dev",
    "build": "npm run prepare:icons && npm run prepare:webapp && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
